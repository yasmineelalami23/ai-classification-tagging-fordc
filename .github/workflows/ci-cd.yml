name: CI/CD Pipeline

# The `paths-ignore` exclusion lists prevent the CI/CD workflow from running on docs or local
# development changes; the `changes` job selects which downstream jobs run when the change set
# passes the workflow-level exclusions. A `paths-ignore` entry must never appear in a `changes`
# filter, or that filter entry is unreachable. Workflow YAML rejects anchors, so the two `paths-ignore`
# lists are duplicated deliberately and must be kept in sync.
on:
  pull_request:
    branches: [main]
    paths-ignore:
      - '*.md'
      - 'docs/**'
      - 'init_template.py'
      - 'mkdocs.yml'
      - 'LICENSE'
      - 'notebooks/**'
      - '.claude/**'
      - '.env.example'
      - '.pre-commit-config.yaml'
  push:
    branches: [main]
    paths-ignore:
      - '*.md'
      - 'docs/**'
      - 'init_template.py'
      - 'mkdocs.yml'
      - 'LICENSE'
      - 'notebooks/**'
      - '.claude/**'
      - '.env.example'
      - '.pre-commit-config.yaml'
    tags:
      - 'v*'

permissions:
  contents: read
  id-token: write
  pull-requests: write
  actions: read  # require-stage-success reads the merge run's job conclusions (a called workflow's token can only be reduced from the caller's, not elevated)

concurrency:
  group: ci-cd-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  changes:
    name: Detect Changes
    # `branch` events only: GitHub skips path filters on tag pushes, and a tag push has no base to diff
    if: github.ref_type == 'branch'
    runs-on: ubuntu-latest
    timeout-minutes: 5  # Typical run: <1 min (path filtering). 5 min is generous buffer
    outputs:
      # `deploy` gates the build-and-deploy chain; `eval` gates the judge gate
      deploy: ${{ steps.filter.outputs.deploy }}
      eval: ${{ steps.filter.outputs.eval }}
    steps:
      - uses: actions/checkout@v6
      - uses: dorny/paths-filter@v4
        id: filter
        with:
          # The action parses this string itself, so YAML anchors work here
          filters: |
            deploy: &deploy
              - 'src/**'
              - 'pyproject.toml'
              - 'uv.lock'
              - 'Dockerfile'
              - '.dockerignore'
              - 'terraform/main/**'
              - '.github/workflows/ci-cd.yml'
              - '.github/workflows/config-summary.yml'
              - '.github/workflows/docker-build.yml'
              - '.github/workflows/metadata-extract.yml'
              - '.github/workflows/pull-and-promote.yml'
              - '.github/workflows/resolve-image-digest.yml'
              - '.github/workflows/require-stage-success.yml'
              - '.github/workflows/smoke.yml'
              - '.github/workflows/terraform-plan-apply.yml'
            eval:
              - *deploy
              - 'tests/eval/**'
              - '.github/workflows/judge-eval.yml'

  meta:
    name: Extract Metadata
    uses: ./.github/workflows/metadata-extract.yml
    secrets: inherit

  config:
    name: Workflow Configuration
    uses: ./.github/workflows/config-summary.yml
    with:
      # Set production_mode: true to enable full production pipeline (dev → stage → prod), false for dev-only
      production_mode: false
    secrets: inherit

  build:
    name: Build Docker Image
    # Skip on tag events (image already exists from merge) and when nothing deployable changed; every deploy job needs this one, so skipping here skips the whole chain
    if: github.ref_type != 'tag' && needs.changes.outputs.deploy == 'true'
    needs: [changes, meta]
    uses: ./.github/workflows/docker-build.yml
    with:
      environment: dev
      push: true
      tags: ${{ needs.meta.outputs.tags }}
    secrets: inherit

  # Resolve digest on tag events (for prod deployment)
  resolve-digest:
    name: Resolve Image Digest
    if: needs.config.outputs.production_mode == 'true' && github.ref_type == 'tag'
    needs: [config, meta]
    uses: ./.github/workflows/resolve-image-digest.yml
    with:
      environment: stage  # Look up image in stage registry (already validated)
      tags: ${{ needs.meta.outputs.tags }}
    secrets: inherit

  # Dev environment (branch events only, not tags)
  dev-plan:
    name: Plan Dev
    if: github.ref_type == 'branch'
    needs: build
    uses: ./.github/workflows/terraform-plan-apply.yml
    with:
      environment: dev
      docker_image: ${{ needs.build.outputs.digest_uri }}
      terraform_action: plan
      save_plan: ${{ github.event_name == 'push' }}  # Save on merge, not PR
    secrets: inherit

  dev-apply:
    name: Apply Dev
    # To enable PR deploys: remove '&& github.event_name == 'push'' from condition below
    if: github.ref_type == 'branch' && github.event_name == 'push'
    needs: [build, dev-plan]
    uses: ./.github/workflows/terraform-plan-apply.yml
    with:
      environment: dev
      docker_image: ${{ needs.build.outputs.digest_uri }}
      terraform_action: apply
      use_saved_plan: ${{ github.event_name == 'push' }}  # Matches the dev-plan job's save_plan pattern
    secrets: inherit

  dev-smoke:
    name: Smoke Dev
    if: github.ref_type == 'branch' && github.event_name == 'push'
    needs: dev-apply
    uses: ./.github/workflows/smoke.yml
    with:
      environment: dev
      service_url: ${{ needs.dev-apply.outputs.smoke_target_url }}
      invoker_service_account: ${{ needs.dev-apply.outputs.smoke_invoker_service_account_email }}
    secrets: inherit

  # Behavioral judge gate (merge to main only, blocking in production mode)
  judge-eval:
    name: Judge Eval
    # The `eval` filter lets an eval-data-only merge score itself while build and deploy skip
    if: github.ref_type == 'branch' && github.event_name == 'push' && needs.changes.outputs.eval == 'true'
    needs: [changes, config]
    uses: ./.github/workflows/judge-eval.yml
    with:
      environment: dev
      gating: ${{ needs.config.outputs.production_mode == 'true' }}
    secrets: inherit

  # Stage environment (merge to main only, not PRs or tags, wait for dev to deploy)
  stage-promote:
    name: Promote to Stage
    if: needs.config.outputs.production_mode == 'true' && github.ref_type == 'branch' && github.event_name == 'push'
    needs: [config, meta, build]
    uses: ./.github/workflows/pull-and-promote.yml
    with:
      source_environment: dev
      target_environment: stage
      source_digest: ${{ needs.build.outputs.digest }}
      target_tags: ${{ needs.meta.outputs.tags }}
    secrets: inherit

  stage-plan:
    name: Plan Stage
    if: needs.config.outputs.production_mode == 'true' && github.ref_type == 'branch' && github.event_name == 'push'
    needs: [config, stage-promote]
    uses: ./.github/workflows/terraform-plan-apply.yml
    with:
      environment: stage
      docker_image: ${{ needs.stage-promote.outputs.digest_uri }}
      terraform_action: plan
      save_plan: true
    secrets: inherit

  stage-apply:
    name: Apply Stage
    if: needs.config.outputs.production_mode == 'true' && github.ref_type == 'branch' && github.event_name == 'push'
    needs: [config, stage-promote, stage-plan]
    uses: ./.github/workflows/terraform-plan-apply.yml
    with:
      environment: stage
      docker_image: ${{ needs.stage-promote.outputs.digest_uri }}
      terraform_action: apply
      use_saved_plan: true
    secrets: inherit

  stage-smoke:
    name: Smoke Stage
    if: needs.config.outputs.production_mode == 'true' && github.ref_type == 'branch' && github.event_name == 'push'
    needs: [config, stage-apply]
    uses: ./.github/workflows/smoke.yml
    with:
      environment: stage
      service_url: ${{ needs.stage-apply.outputs.smoke_target_url }}
      invoker_service_account: ${{ needs.stage-apply.outputs.smoke_invoker_service_account_email }}
    secrets: inherit

  # Production environment (tag events only)
  require-stage-success:
    name: Require Stage Success
    if: needs.config.outputs.production_mode == 'true' && github.ref_type == 'tag'
    needs: [config]
    uses: ./.github/workflows/require-stage-success.yml
    with:
      head_sha: ${{ github.sha }}
    secrets: inherit

  prod-promote:
    name: Promote to Prod
    if: needs.config.outputs.production_mode == 'true' && github.ref_type == 'tag'
    needs: [config, resolve-digest, require-stage-success]
    uses: ./.github/workflows/pull-and-promote.yml
    with:
      source_environment: stage  # Promote from stage (validated release candidate)
      target_environment: prod
      source_digest: ${{ needs.resolve-digest.outputs.digest }}
      target_tags: ${{ needs.resolve-digest.outputs.tags }}
    secrets: inherit

  prod-plan:
    name: Plan Prod
    if: needs.config.outputs.production_mode == 'true' && github.ref_type == 'tag'
    needs: [config, prod-promote]
    uses: ./.github/workflows/terraform-plan-apply.yml
    with:
      environment: prod
      terraform_environment: prod
      docker_image: ${{ needs.prod-promote.outputs.digest_uri }}
      terraform_action: plan
      save_plan: true
    secrets: inherit

  prod-apply:
    name: Apply Prod
    if: needs.config.outputs.production_mode == 'true' && github.ref_type == 'tag'
    needs: [config, prod-promote, prod-plan]
    uses: ./.github/workflows/terraform-plan-apply.yml
    with:
      environment: prod-apply
      terraform_environment: prod
      docker_image: ${{ needs.prod-promote.outputs.digest_uri }}
      terraform_action: apply
      use_saved_plan: true
    secrets: inherit

  prod-smoke:
    name: Smoke Prod
    if: needs.config.outputs.production_mode == 'true' && github.ref_type == 'tag'
    needs: [config, prod-apply]
    uses: ./.github/workflows/smoke.yml
    with:
      environment: prod
      service_url: ${{ needs.prod-apply.outputs.smoke_target_url }}
      invoker_service_account: ${{ needs.prod-apply.outputs.smoke_invoker_service_account_email }}
    secrets: inherit
